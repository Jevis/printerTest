import EscPosEncoder from "esc-pos-encoder";
import { PrintData } from "./text/Text";

export default function AddLine(props:{addEncoderListen:(data:PrintData)=>void}){
    return (
        <div style={{height:'300px',width:'300px',backgroundColor:'red'}}>

            AddLine
        </div>
    );
}